import { Controller,Get,Param,ParseUUIDPipe } from '@nestjs/common';import { ApiTags } from '@nestjs/swagger';import { ProtocolQueryService } from '../services/protocol-query.service.js';@ApiTags('protocols') @Controller('clients') export class ClientProtocolsController {constructor(private readonly query:ProtocolQueryService){} @Get(':id/protocols')byClient(@Param('id',ParseUUIDPipe)id:string){return this.query.byClient(id)}}
