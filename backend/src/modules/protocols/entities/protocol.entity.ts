import type { Protocol, ProtocolSession } from '../../../generated/prisma/client.js';
export function toProtocolEntity(model: Protocol) { return model; }
export function toProtocolSessionEntity(model: ProtocolSession) { return model; }
