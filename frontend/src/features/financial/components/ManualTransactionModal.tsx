import { listResourceAccounts } from '../api/cash-api';
import type { ResourceAccount } from '../types/cash';
import { useEffect,useState,type FormEvent } from 'react';
import { describeApiError } from '../../../shared/api/http-client';
import { Button } from '../../../shared/components/Button';import { Input } from '../../../shared/components/Input';import { Modal } from '../../../shared/components/Modal';import { Select } from '../../../shared/components/Select';import { parseBRLToCents } from '../../../shared/utils/format';
import { createManualTransaction } from '../api/financial-api';import { FINANCIAL_STATUSES,PAYMENT_METHODS,TRANSACTION_TYPES,type FinancialStatus,type PaymentMethod,type TransactionType } from '../types/financial';
export function ManualTransactionModal({open,onClose,onSaved}:{open:boolean;onClose:()=>void;onSaved:()=>void}){const[accounts,setAccounts]=useState<ResourceAccount[]>([]);const[resourceAccountId,setResourceAccountId]=useState('');const[description,setDescription]=useState('');const[category,setCategory]=useState('');const[value,setValue]=useState('');const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[type,setType]=useState<TransactionType>('RECEITA');const[status,setStatus]=useState<FinancialStatus>('PAGO');const[paymentMethod,setPaymentMethod]=useState<PaymentMethod>('PIX');const[error,setError]=useState<string|null>(null);const[saving,setSaving]=useState(false);useEffect(()=>{if(open)listResourceAccounts().then(setAccounts).catch(e=>setError(describeApiError(e)));},[open]);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError(null);try{await createManualTransaction({description,category:category||undefined,resourceAccountId:resourceAccountId||undefined,amountCents:parseBRLToCents(value),date,paymentMethod,type,status});onSaved();onClose();}catch(x){setError(describeApiError(x));}finally{setSaving(false);}}return <Modal open={open} onClose={onClose} title="Novo lançamento"><form onSubmit={submit} className="space-y-4"><Input label="Descrição" required value={description} onChange={e=>setDescription(e.target.value)}/><div className="grid gap-4 sm:grid-cols-2"><Select label="Tipo" value={type} onChange={e=>setType(e.target.value as TransactionType)} options={Object.entries(TRANSACTION_TYPES).map(([value,label])=>({value,label}))}/><Input label="Valor (R$)" required inputMode="decimal" value={value} onChange={e=>setValue(e.target.value)}/><Input label="Data" required type="date" value={date} onChange={e=>setDate(e.target.value)}/><Select label="Situação" value={status} onChange={e=>setStatus(e.target.value as FinancialStatus)} options={Object.entries(FINANCIAL_STATUSES).filter(([v])=>v!=='CANCELADO').map(([value,label])=>({value,label}))}/><Select label="Forma de pagamento" value={paymentMethod} onChange={e=>setPaymentMethod(e.target.value as PaymentMethod)} options={Object.entries(PAYMENT_METHODS).map(([value,label])=>({value,label}))}/><Select label="Local do recurso" value={resourceAccountId} onChange={e=>setResourceAccountId(e.target.value)} options={[{value:'',label:'A definir'},...accounts.map(a=>({value:a.id,label:a.name}))]}/><Input label="Categoria" value={category} onChange={e=>setCategory(e.target.value)}/></div>{error&&<p className="text-sm text-rose-600">{error}</p>}<div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={onClose}>Cancelar</Button><Button type="submit" disabled={saving}>{saving?'Salvando…':'Salvar lançamento'}</Button></div></form></Modal>}
