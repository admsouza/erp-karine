# URL base da API consumida pelo frontend.
# Em desenvolvimento o padrão '/api' usa o proxy do Vite para http://localhost:3001.
# Em produção a API fica na mesma origem, então normalmente não é necessário definir.
VITE_API_URL=/api
